base:
  model: yolo11n-seg.pt
  data: configs/data.yaml
  epochs: 50
  imgsz: 640
  batch: 8
  device: 0
  project: runs/ablation
  seed: 42

experiments:
  - name: baseline
    overrides: {}
  - name: image_960
    overrides:
      imgsz: 960
  - name: stronger_augmentation
    overrides:
      mosaic: 1.0
      mixup: 0.15
      copy_paste: 0.2
