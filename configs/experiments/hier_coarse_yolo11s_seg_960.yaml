project_root: ../..
model: yolo11s-seg.pt
data: datasets/blade-v2-6class/data.yaml
epochs: 3
imgsz: 960
batch: 8
device: auto
workers: 4
project: runs
name: hier_coarse_yolo11s_seg_960_smoke
patience: 30
seed: 42
pretrained: true
cache: false
amp: true
val: true
save: true
exist_ok: true
notes: >
  6 类粗粒度有效 smoke（1—3 epochs）。数据集为修复索引后的
  blade-v2-sampled-4987-6class（train 3,991 / val 996，5,099 实例，6 类齐全），
  旧 0.316 结果为路径错配导致的无效结果，不进入正式汇总。
  正式 6 类训练（50 epochs）在 smoke 验证标签读取正确后另行启动。
