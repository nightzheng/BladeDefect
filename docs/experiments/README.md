# 实验文档索引

本目录按用途分为“交接入口、训练计划、历史实验记录”三类。为避免破坏已有引用，暂不移动文件，以本索引完成分类。

## OBB 交接入口

- `obb_handoff_guide.md`：OBB 主交接指南、当前状态与后续执行顺序。
- `obb_sample_activation_handoff.md`：正式 v3 索引上的 120 张分层抽样转换、validator、预览和 sample-smoke 数据入口。

## 训练计划

- `v3_training_plan.md`：v3 正式训练计划。
- `full_training_plan.md`：完整训练计划与资源安排。

## 历史实验记录

- `exp001.md`
- `exp008.md`

新增实验文档时，应优先更新本索引；OBB 当前状态只在 `obb_handoff_guide.md` 维护，具体一次性验收证据放入独立 handoff 或 results 目录，避免多个文件同时声明不同状态。
